import React from "react";
import { View, StyleSheet, Text } from "react-native";
const SearchBar = () => {
  return (
    <View>
      <Text>SearchBar</Text>
    </View>
  );
};

export default SearchBar;
